function solution(number) {
  if (number <= 0) {
    return 0;
  }

  function isDivisible(num, base) {
    return num % base === 0;
  }
  let arr = []

  for (let i = number; i > 0; i--) {
      arr.map

  }

  let result = arr.reduce((acc, item) => acc * item, 1);

  return result;
}
